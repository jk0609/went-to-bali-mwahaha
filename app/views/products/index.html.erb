<h3>Products</h3>

<div class="row">
  <div class="section">
  <% @products.each do |product| %>
    <div class='col s4'>
      <%= render 'order', :product=>product %>
      <%= render 'detail', :product=>product  %>
    </div>
  <% end %>
</div>
</div>
